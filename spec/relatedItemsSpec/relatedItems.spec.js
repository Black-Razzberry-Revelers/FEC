const Related = require('../../client/src/components/Related/Related').default;

describe('Related Items Parent Component', () => {
  test('Expect Related to be a function', () => {
    expect(typeof Related).toBe('function');
  });
});
